name: Health Check

on:
  schedule:
    # Check every 15 minutes
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  health-check:
    name: 🏥 Production Health Check
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        
      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          
      - name: 📚 Install dependencies
        run: npm ci
        
      - name: 🏥 Check application health
        run: |
          # Get the production URL from Vercel deployment
          PROD_URL="https://bangerrequest-8sasrdwae-mommys-projects-f4f4fbbb.vercel.app"
          # Note: basePath /richiedi is configured in next.config.ts but may not be active on this deployment
          # API routes NEVER use basePath on Vercel (they're always at /api/*)
          # Vercel Protection Bypass for CI/CD (requires VERCEL_PROTECTION_BYPASS secret)
          BYPASS_PARAM=""
          if [ -n "${{ secrets.VERCEL_PROTECTION_BYPASS }}" ]; then
            BYPASS_PARAM="?x-vercel-set-bypass-cookie=true&x-vercel-protection-bypass=${{ secrets.VERCEL_PROTECTION_BYPASS }}"
          fi
          
          echo "🔍 Checking health endpoints..."
          echo "Production URL: ${PROD_URL}"
          echo ""
          
          # Check main health endpoint (API routes don't use basePath)
          echo "Checking ${PROD_URL}/api/health..."
          response=$(curl -s -o /dev/null -w "%{http_code}" "${PROD_URL}/api/health${BYPASS_PARAM}" || echo "000")
          if [ "$response" = "200" ]; then
            echo "✅ Main health check passed"
          else
            echo "❌ Main health check failed (HTTP $response)"
            exit 1
          fi
          
          # Check Supabase health
          echo "Checking ${PROD_URL}/api/health/supabase..."
          response=$(curl -s -o /dev/null -w "%{http_code}" "${PROD_URL}/api/health/supabase${BYPASS_PARAM}" || echo "000")
          if [ "$response" = "200" ]; then
            echo "✅ Supabase health check passed"
          else
            echo "⚠️ Supabase health check failed (HTTP $response)"
          fi
          
          # Check Spotify health
          echo "Checking ${PROD_URL}/api/spotify/health..."
          response=$(curl -s -o /dev/null -w "%{http_code}" "${PROD_URL}/api/spotify/health${BYPASS_PARAM}" || echo "000")
          if [ "$response" = "200" ]; then
            echo "✅ Spotify health check passed"
          else
            echo "⚠️ Spotify health check failed (HTTP $response)"
          fi
          
          # Check main page loads (check root since basePath may not be active)
          echo "Checking ${PROD_URL}/..."
          response=$(curl -s -o /dev/null -w "%{http_code}" "${PROD_URL}/${BYPASS_PARAM}" || echo "000")
          if [ "$response" = "200" ]; then
            echo "✅ Homepage loads successfully"
          else
            echo "❌ Homepage failed to load (HTTP $response)"
            exit 1
          fi
          
          echo ""
          echo "🎉 Health check completed successfully!"
          
      - name: 📊 Run detailed health check
        run: |
          # API routes don't use basePath on Vercel
          node scripts/check_health.cjs https://bangerrequest-8sasrdwae-mommys-projects-f4f4fbbb.vercel.app/api/health/supabase
          
      - name: 💬 Notify on failure
        if: failure()
        run: |
          echo "❌ Health check failed! Production may be down."
          echo "Check the application immediately: https://bangerrequest-8sasrdwae-mommys-projects-f4f4fbbb.vercel.app"